# 홀덤펍 키우기 작업 메모

새 세션을 시작하면 아래 "진행 상황 로그"부터 읽고 이어서 작업할 것. 세션을 마칠 때는 맨 위에 날짜와 함께 새 항목을 추가한다 (무엇을 했는지 / 다음에 할 일).

## 진행 상황 로그

### 2026-09-18 (16차) — 명성 단계별 매장 구조 변화 + 남은 정리

- **요청**: "구조적인 요소 추가하고, 특정 레벨/단계 달성하면 단순히 넓어지는 게 아니라 구조가 달라지게. 그리고 남은 작업 전부 완료."
- **매장 구조 단계 신설**(`VENUE_STAGES`, scene3d.js): game.js의 `BUILDING_STEPS`(명성 0/5/20/50 · pub/club/premium/empire)를 **그대로 재사용**한다 — 리뉴얼 탭에 뜨는 외관 그림과 실제 매장이 따로 놀면 안 되므로. `renderScene()`이 `stage`를 스냅샷에 실어 보낸다.
  - 0 로컬 펍: 기본
  - 1 인기 클럽: **대형 벽면 사인**(빛나는 패널 + 글자 대신 막대) + 입구 어닝 + 레드 러너
  - 2 프리미엄 하우스: + 대형 샹들리에 + 기둥 + 벨벳 로프 게이트 + 트로피 진열장
  - 3 포커 제국: + **2층 메자닌**(슬래브·난간·계단·VIP 소파·VIP 사인) + 입구 골드 아치
- **배치가 까다로웠던 이유 3가지** (다음에 구조물을 더 붙일 때 그대로 적용됨):
  1. **바닥에 여유가 없다.** 테이블 그리드가 x ±5.4를 쓰고 테이블 하나가 x로 ±2.75를 먹어 벽까지 거의 꽉 찬다 → 새 구조물은 벽·천장·2층 등 테이블이 놓이지 않는 곳에만. 메자닌도 뒷벽 **오른쪽 위**로 올려 바 카운터(뒷벽 왼쪽 `-halfW+3`)를 가리지 않게 했다.
  2. **카메라 시야가 고정**(`BASE_HALF_H`)이라 매장이 커지면 벽은 화면 밖으로 나간다 → 구조물은 벽이 아니라 중앙 기준(`VX`/`VZF`로 상한)에 둔다. 처음엔 벽에 붙였다가 큰 매장에서 전부 화면 밖으로 사라졌다.
  3. **아이소메트릭에서는 높이 올릴수록 화면 "위"(=뒤쪽 바닥 자리)로 간다.** 샹들리에를 매장 중앙에 달았더니 화면에선 뒤쪽 테이블을 덮었다 → 앞쪽(`ROOM_CENTER_Z + 4.2`)에 매달아야 화면 한가운데 빈 바닥 위에 걸린 것처럼 보인다.
  - 기둥은 바 카운터와 겹치면 자동으로 오른쪽으로 비킨다(매장 크기에 따라 바 위치가 달라지므로 고정 좌표로는 못 피한다).
- **남은 작업 정리**: 유럽풍 기둥·벽난로가 바 카운터와 겹치던 것 수정(뒷벽 오른쪽으로), **펜던트 조명 갓을 테마별로**(`PAL.lampShade` — 일본풍은 주칠, 네온은 암자색), **술병 색도 테마별로**(`themeDef.bottles` — 네온은 형광, 일본풍은 사케 계열).
- **캐시 버전 ?v=16**.
- **검증**: Playwright로 단계 0~3 + 테마별 3단계 조합 8장 + 실제 게임 시작 상태 캡처. 페이지 에러 0건.
- **남은 것**: (1) 포스터의 대형 벽면 문구는 글자가 아니라 막대로 흉내 냈다 — 진짜 글자를 넣으려면 캔버스 텍스처가 필요하다. (2) `assets/raw` 31MB가 `188a36e` 히스토리에 남아 있다. 이미 푸시된 브랜치라 지우려면 히스토리 재작성 + force push가 필요해서 **임의로 하지 않았다** — 진행 여부는 사용자 결정 대기.

### 2026-09-18 (15차) — 매장 밖 풍경 + 테마별 전용 인테리어 소품

- **제보**: "모든 테마가 너무 대충인 것 같아. 매장 밖 배경도 없고, 인테리어 컨셉에 맞춰 디테일한 에셋 만들어와." (+ 포스터 재첨부)
- 14차는 **색만** 바꿨다 — 같은 매장에 페인트만 칠한 셈이라 컨셉이 안 느껴졌고, 벽 너머는 하늘 그라데이션뿐이라 매장이 허공에 떠 있었다.
- **매장 밖 동네 신설**(`rebuildOutside`): 바닥 지면 + 테마별 소품. 클래식=집·나무·가로등 / 공주=토피어리·울타리 / 유럽=석조 타워·사이프러스 / 네온=네온 창문 고층 스카이라인 / 일본=벚나무·대나무·석등·기와집.
  - **배치가 이 작업의 전부였다.** 카메라가 (+x, +z) 위에 있어서 그 방향에 깐 소품은 매장 "앞"을 가로막는다(처음엔 네온 빌딩이 매장 한가운데를 통째로 가렸다). → 구역을 둘로 나눔:
    - `near`(-40°~130°, 카메라 쪽) — 화면 아래 여백에 깔리도록 멀리. **키가 클수록 더 멀리**(집·빌딩은 `radRel`로 따로 지정) 안 그러면 위로 뻗어 매장을 찌른다.
    - `far`(150°~300°, 벽 뒤) — 벽 높이 `WALL_H`(7)를 넘는 것만 크게 키워 지붕 위로 빼꼼 보이게.
  - 거리는 전부 매장 반지름 `R0` 기준 상대값 — 매장을 확장해도 동네가 안쪽으로 파고들지 않는다.
  - **시드 고정 난수**(`seeded()`)를 쓴다. 매번 무작위로 깔면 업그레이드할 때마다(=update()가 씬을 다시 만들 때마다) 바깥 동네가 통째로 움직여서 어지럽다.
- **테마 전용 인테리어**(`rebuildThemeDecor`, `THEME_DECOR` 배치표): 클래식=머그 걸이·맥주 탭·나무 간판·액자 / 공주=꽃 아치·장미 화병·샹들리에·파스텔 액자 / 유럽=벽난로·기둥·촛대·금테 액자 / 네온=네온 사인·디스코볼 / 일본=노렌·쇼지 창·사케통·분재·석등.
- **캐시 버전 ?v=15**.
- **검증**: Playwright로 5개 테마 + 실제 게임 시작 상태 캡처. 페이지 에러 0건.
- **다음 할 일**: (1) 유럽풍 기둥이 바 카운터와 겹치는 각도가 있다 — 자리 조정 필요. (2) 펜던트 조명 갓은 아직 전 테마 공통(황동). (3) 술병 선반 병 색은 테마 무관 고정. (4) 프레스티지 건물 4단계는 여전히 대응하는 게임 단계 개념이 없다(12차부터). (5) `assets/raw` 31MB가 `188a36e` 히스토리에 남아 있는 건 그대로.

### 2026-09-18 (14차) — 테마별 기물·테이블·딜러·손님 + 일본풍 추가

- **요청**: "기물들 테이블 딜러 손님 에셋 만들자. 일반 공주 유럽 네온 그리고 일본 추가."
- **먼저 리팩터링**(별도 커밋 a5d63b8): 기물·가구 빌더가 전역 `COLORS`를 직접 읽어 테마별로 색을 바꿀 수 없었다 → `COLORS`는 기본값으로 두고 빌더가 읽는 살아있는 팔레트 `PAL`을 분리(참조 57곳). **주의: `scene3d.js`에 이미 지역 변수 `P`가 3개 있고 그중 하나가 테이블 빌더 안이라, 팔레트를 `P`로 두면 가려진다** — 그래서 `PAL`.
- **THEMES가 매장 전체의 "옷 한 벌"이 됨**: 기존 floor/wall/trim/felt에 더해
  - `props` — `COLORS` 위에 덮어쓰는 기물·가구 색(레일·의자·스툴·바·냉장고·조명·빈자리 표시·직원복)
  - `customers` — 손님 상의 팔레트
  - `dealerTint`/`dealerMix` — 딜러 정장을 테마 쪽으로 섞는 정도. **보타이·바이저(accent)는 등급색 그대로 둬서 매장에서 등급을 계속 알아볼 수 있게 했다.**
  - `lights` — `"bulb"`(전구 줄) / `"lantern"`(제등)
  `applyTheme()`이 `Object.assign(PAL, COLORS, t.props)`로 갈아끼우면, update()가 테이블·기물·장식을 다시 만들면서 매장 전체가 새 색으로 그려진다. **항상 COLORS부터 다시 깔아야** 이전 테마 색이 남지 않는다.
- **손님은 그 자리에서 갈아입힌다**: 새로 오는 손님만 바꾸면 한동안 두 테마 색이 섞여 보인다 → `makePersonMesh`가 `group.userData.torso`에 상의 메시를 남기고, `applyTheme`이 이미 있는 손님 전원의 색을 바로 바꾼다(손님 1명 = 서 있는/앉은 메시 2개라 둘 다).
- **🏮 일본풍 신규**(`D.themes`에 cost 30000): 다다미 바닥 + 격자 크림 벽 + 제등 + 남색/주칠 펠트 + 붉은 스툴. 다다미 패턴을 `makeFloorTexture`에 추가.
  - **함정**: 헤리(가선)를 위아래만 그렸더니 텍스처가 늘어나면서 그냥 줄무늬 바닥이 됐다 → 네 변을 다 둘러야 "돗자리 여러 장"으로 보인다.
- **썸네일**: `assets/img/theme/japanese.png` (z_image, 0.15크레딧). `tools/asset-pack.mjs` JOBS에 추가.
- **미사용 `CUSTOMER_SHIRT_COLORS` 제거** — 팔레트는 이제 THEMES가 들고 있다.
- **캐시 버전 ?v=14**.
- **검증**: Playwright로 `PubScene3D.update()`에 테마만 바꾼 스냅샷을 넣어 5종 전부 캡처. 페이지 에러 0건. 각 테마가 바닥·벽·테이블·의자·딜러·손님까지 확실히 구분되는 것 확인.
- **다음 할 일**: (1) 펜던트 조명 갓은 아직 전 테마 공통(황동) — 일본풍은 제등 모양으로 바꾸면 더 살 것. (2) 술병 선반의 병 색은 테마와 무관하게 고정이다. (3) 프레스티지 건물 4단계는 여전히 게임에 대응하는 단계 개념이 없다(12차부터 이어짐). (4) `assets/raw` 31MB가 `188a36e` 히스토리에 남아 있는 건 그대로 — 정리 여부 미정.

### 2026-09-18 (13차) — 3D 매장을 포스터 톤으로 (딜러·테이블·매장)

- **제보**: "커밋푸시해봤는데 딜러나 테이블 매장은 바뀌지 않았어." 맞다 — 12차의 에셋과 2c4070f의 연결 작업은 **전부 HUD/시트 쪽 UI**였고, 매장 안(딜러·테이블·바닥·벽)은 `scene3d.js`가 Three.js로 직접 만드는 지오메트리라 이미지 에셋과 아무 상관이 없다. 그래서 이번엔 씬 팔레트 자체를 포스터에 맞췄다.
- **팔레트**(`COLORS` + `THEMES`): 바닥 겨자색 `0xf3c988` → 밝은 우드 `0xefd8b8`, 테이블 레일 진갈색 `0x7a4a33` → `0xb88055`, 의자·스툴 적갈색 `0xc9506b` → **핑크 벨벳 `0xff9db5`**, 바·소파·우드 전반을 한 단계씩 밝게, 펠트를 더 선명한 초록으로. 4개 테마 전부 같은 방향으로 손봤다.
- **조명**: `HemisphereLight`의 바닥 반사광이 진한 주황(`0xd9a35f`)이라 우드 바닥이 겨자색으로 떴다 → 크림(`0xf0dcc4`)으로 낮추니 팔레트가 의도대로 나온다. 원인을 못 찾고 바닥 색만 계속 바꾸면 영영 안 맞는 부분.
- **벽 우드 패널(웨인스코팅) 추가**: 포스터 펍의 핵심 요소. 기존 핑크 트림(y=1.3)이 마침 갓돌 위치라 그 아래를 우드로 채웠다. 좌우 벽에도 같은 트림을 붙였다.
  - **함정**: 처음에 Box로 만들었더니 우측 벽 쪽에서 바닥 가장자리에 갈색 판때기가 튀어나왔다. 벽은 `PlaneGeometry`(단면)이라 카메라가 바깥인 면은 안 그려지는데, Box는 바깥면도 그려지기 때문. 벽과 똑같이 Plane + `rotation.y`로 맞춰 해결.
- **펜던트 조명**: 갓이 어두운 남색(`0x2f3550`)이었다 → 황동(`0xd9a441`) + 전구 주변 반투명 halo. 실제 광원(PointLight)을 테이블마다 두면 휴대폰에서 느려지므로 "빛나 보이게"만 한다.
- **바닥 텍스처**: 선이 진해서(`rgba(60,40,30,0.28)`, 굵기 3) 마루가 아니라 벽돌담처럼 보였다 → `rgba(120,85,60,0.16)`·굵기 2, 마루널 이음매 간격도 2배로.
- **기본 화분 2개**: 장식품을 하나도 안 산 초반에 매장이 휑해서, 레벨과 무관하게 좌우 벽에 항상 둔다.
- **`DEALER_RARITY_LOOK`에 `uncommon` 누락 수정**: U 등급 운영진이 `|| common` 폴백에 걸려 일반과 똑같은 회색으로 보이고 있었다. 겸사겸사 accent 색을 `js/assets.js`의 등급 색과 같은 값으로 맞춰서, 도감에서 본 색이 매장에서도 그대로 보인다.
- **캐시 버전 ?v=13**.
- **검증**: Playwright로 firebase-init.js를 가짜 모듈로 가로채 실서버 없이 띄우고 매장 + 업그레이드/대회/상점 탭 캡처. 페이지 에러 0건.
- **다음 할 일**: (1) 바 카운터·냉장고·도어 같은 개별 기물도 아직 예전 톤이 남아 있다 — 필요하면 같은 방식으로 손볼 것. (2) 손님 옷 색(`CUSTOMER_SHIRT_COLORS`)은 이미 포스터 팔레트와 비슷해 손대지 않았다. (3) 프레스티지 건물 4단계는 여전히 게임에 대응하는 단계 개념이 없다(12차에서 이어짐).

### 2026-09-18 (13차) — 12차에서 만든 에셋을 게임에 실제로 연결
- 12차가 만들어만 두고 "게임 연결은 아직"이라고 남긴 것을 연결했다(사용자: "저장소에 에셋 적용해서 배포해봐").
- **`applyStaticAssets()`**(game.js, init에서 1회): 지갑 재화 아이콘 3종은 `GameAssets.dataUri()`를 `background-image`로, 하단 네비 4개·사이드 레일 4개·설정 버튼·테이블 탭 고정 카드 3개는 `GameAssets.icon()` SVG로 교체. 알림 점(`.dot`)은 innerHTML 교체 후 다시 붙인다.
- **목록 렌더에 연결**: 시설(bar/fridge)·직원(bartender/server/marketer) 아이콘, 트로피 상점(moon/target/hall/ticket/shard), 특성 칩·카드 특성 이모지 → `TRAIT_ICON` 매핑 SVG, 대회 목록은 `assets/img/trophy/{tier}-96.png`, 인테리어 테마 카드는 `assets/img/theme/{id}.png`(`onerror`로 없으면 기존 색 스와치), 리뉴얼 탭은 프레스티지 포인트 구간(0/5/20/50)에 맞춰 `assets/img/prestige/{pub,club,premium,empire}-256.png` + 단계 이름. 파비콘·iOS 홈화면 아이콘, 로그인 화면 키비주얼도 연결.
- **에셋 버그 수정(중요)**: `js/assets.js`에서 `${OUT} stroke-width="2.1"`처럼 **같은 속성을 두 번** 쓴 곳이 33군데 있었다. 인라인 SVG로는 브라우저가 첫 값을 쓰고 넘어가지만, **data URI(`background-image`)로 넣으면 XML 파서가 걸려서 아이콘이 통째로 안 그려진다**(지갑 다이아 아이콘이 빈칸으로 나온 원인). `OUTW(굵기, 색)` 헬퍼를 추가해 전부 교체하고, 아이콘 44종·프레임·트로피·건물·로고를 전부 생성해 중복 속성을 검사하는 스크립트로 0건 확인. **앞으로 assets.js에 도형을 추가할 때 `${OUT}` 뒤에 stroke 관련 속성을 또 쓰지 말 것.**
- 검증: Playwright로 지갑 다이아 아이콘 렌더 확인, 시설·인테리어·대회·리뉴얼·설정 탭 스크린샷, 페이지 에러 0건.
- **주의(작업 충돌)**: 11차 커밋(`188a36e`)에서 `git add -A`로 12차 세션의 작업 파일(`js/assets.js`, `tools/asset-pack.mjs`, `assets/**`)이 함께 푸시됐다. 특히 `assets/raw/` 원본 PNG 8장(약 34MB)이 저장소에 들어갔다 — 게임은 읽지 않으니 `.gitignore`로 빼는 게 좋다(사용자 결정 대기). **여러 세션이 같은 저장소를 만질 때는 `git add -A` 대신 고친 파일만 지정할 것.**

### 2026-09-18 (12차) — 포스터 키비주얼 기반 에셋 세트

- **요청**: 홍보 이미지(포스터) 2장을 주며 "이미지를 토대로 타이쿤 게임 에셋 만들어와". 방식은 SVG·AI PNG **둘 다**, 범위는 UI 아이콘 / 등급 카드 프레임 / 트로피·프레스티지 건물 / 로고·앱아이콘·로딩화면 **4종 전부**.
- **분담 기준**: 반복해서 작게 뜨는 것(아이콘·프레임·배지)은 **SVG 코드**, 크게 한 번 보여주는 그림(트로피·건물·테마·앱아이콘·로딩아트)은 **PNG**. 이 프로젝트는 지금까지 이미지 에셋이 0개였고(3D는 Three.js 생성, 초상화는 portraits.js가 SVG) 4차 때 다운로드 용량을 절반으로 줄인 이력이 있어서, 무조건 PNG로 가면 그 작업을 되돌리게 된다.
- **새 파일**
  - `js/assets.js` — `GameAssets`. 아이콘 44종 + 등급 프레임/배지 6종 + 트로피 5단계 + 건물 4단계 + 로고 + 앱아이콘. **그라데이션을 안 쓴다** — 같은 화면에 인라인 SVG가 수십 개 깔리면 `<linearGradient>` id가 충돌한다. 셀셰이딩은 원래 "단색 면 + 하이라이트 도형"이라 아트 방향과도 맞고 마크업도 절반.
  - `css/assets.css` — 크기 프리셋, 재화 표기(`.ga-cur`), 등급 광채, 로딩 화면.
  - `assets-preview.html` — 전체 에셋 시트 + 호출 코드. 적용 예시(HUD/네비)도 같이 그린다.
  - `tools/asset-pack.mjs` — 원본 PNG → 배경 제거 → 크롭 → 축소. **외부 패키지 없이 Node 내장 zlib만** 쓴다(PNG 디코더·인코더 직접 구현).
  - `assets/README.md` — 위 내용 정리.
- **PNG 21개** (`assets/img/`, 합계 2.2MB): 트로피 5단계 × 2사이즈, 프레스티지 건물 4단계 × 2사이즈, 인테리어 테마 4종, 앱 아이콘 4사이즈, 로딩 디오라마 2사이즈. 원본은 `assets/raw/`(63MB) — 게임은 안 읽으니 배포에서 빼도 된다.
- **이미지 생성**: 계정이 무료 플랜(10크레딧)이라 Recraft(유료 전용) 대신 `z_image`(장당 0.15크레딧)로 총 16장 생성, 약 2.4크레딧 사용. 동시 제출은 6장쯤에서 429가 나므로 나눠 던져야 한다.
- **배경 제거에서 막힌 것**: 생성 이미지의 "흰 배경"이 실제로는 **옅은 회색·베이지 비네트**라 순백 기준 flood fill이 테두리 근처에서 멈춰 사각형 배경이 그대로 남았다. 이웃 색 비교(adaptive)로 바꿨더니 이번엔 밝은 건물·트로피를 먹어치웠다. 결국 **밝기 + 채도** 기준(`pale` 모드, 밝기 ≥ 172 · 채도 ≤ 42)으로 해결 — 회색 배경은 채도가 낮고 물체는 채도가 높으며, 진한 아웃라인이 flood fill을 막아준다. 핑크 그라데이션 배경(로딩 아트)만 `adaptive`를 쓴다.
- **용량**: 로딩 키아트를 통짜로 넣으면 2MB였다. 배경 그라데이션을 벗겨 디오라마만 남기고 배경은 CSS(`.ga-loading`)가 그리게 해서 153KB로 줄임 — 화면 비율이 달라도 안 잘리는 부수 효과도 있다. PNG 인코더에 적응형 스캔라인 필터를 넣어 전체적으로 20~30% 더 줄였다.
- **앱 아이콘**: 모델이 프롬프트로 아무리 막아도 바깥에 보라색 테두리를 넣어서, `crop` 옵션(0.1~0.9)으로 잘라냈다.
- **검증**: Playwright로 `assets-preview.html`을 띄워 렌더 확인(페이지 에러 0건). 1차 렌더에서 형태가 안 읽히던 아이콘 6종(BB칩·바텐더·서빙직원·두뇌·업그레이드·상점)과 광선이 안 보이던 등급 프레임을 고치고 재확인. 두뇌는 뇌 그림이 작은 크기에서 안 읽혀 **전구**로 바꿨다.
- **다음 할 일**: (1) **게임에 연결하는 작업은 아직 안 했다** — `index.html`·`game.js`의 이모지를 `GameAssets.icon()`으로 갈아끼우기. game.js가 3,300줄이고 0.5초마다 목록을 다시 그리는 구조라 탭 하나씩 바꾸며 확인할 것. (2) 도감/가챠 카드에 `rarityFrame` + portraits.js 초상화 합치기. (3) 대회 탭 트로피 PNG, 인테리어 탭 테마 썸네일 연결. (4) 프레스티지 건물 4단계는 게임에 대응하는 단계 개념이 아직 없다 — 프레스티지 포인트 구간에 매핑하든지 별도 설계 필요.

### 2026-09-18 (11차) — 설정 정리 + 효과음
- **설정 탭 정리(사용자 요청 "저장 데이터 없애고 계정/음량 같은 필요한 것만")**: 저장 데이터 상자(내보내기·가져오기·전체 초기화·저장 코드 textarea)와 `resetGame()` 삭제. 남은 건 **계정** / **게임 설정**(효과음 음량, 코인 애니메이션, 운영진 자동 편성)뿐. `GameBackend.resetState/exportState/importState`는 그대로 있으니 버튼만 다시 붙이면 되살아난다. (진행이 줄었을 때만 뜨는 "🛟 백업 복구" 상자는 안전장치라 남겨둠 — 평소엔 hidden)
- **효과음 신설 `js/sfx.js`**: 오디오 파일 없이 WebAudio 오실레이터로 합성(다운로드 0KB). 브라우저 정책 때문에 첫 pointerdown/keydown/touchend에서 AudioContext를 만들고 resume. 음량은 `state.settings.sfxVolume`(기본 0.6) → 설정 슬라이더. 소리 종류: buy(구매·강화) / reward(보상 — `grantReward()` 한 곳에서 퀘스트·미션·출석·선물 전부 커버) / gacha(등급이 높을수록 음이 길고 화려) / star(승급·레벨업) / gem(💎 말풍선) / boost / win·lose(대회 결과).
- 실기기 확인 필요: 효과음이 너무 잦거나 시끄러우면 `SOUNDS`의 gain을 낮추거나 buy 사운드를 연타 시 throttle 할 것.

### 2026-09-18 (10차) — 진행 날아가는 버그 + 연타하다 화면 확대되는 문제
- **제보**: "업그레이드하려고 터치를 막 하다가 가끔 전체가 확대되고, 새로고침하면 데이터가 전부 날아간다."
- **데이터 소실 원인(backend.js)** — 두 군데가 겹쳤다.
  1. `loadState()`가 **클라우드 문서가 없거나 비어 있으면 무조건 `state: null`**(=새 게임)을 돌려줬다. 이 기기에 멀쩡한 로컬 캐시가 있어도 무시 → 새 게임으로 시작 → 10초 뒤 자동 저장이 그 빈 상태로 **로컬·클라우드를 모두 덮어써서 영구 소실**.
  2. 클라우드 읽기가 실패(6초 타임아웃)해 로컬로 시작하면, 이후 저장이 "클라우드가 더 최신"이라며 계속 거부됐다(`cloudVerified`가 계속 false) → 그 세션 진행이 클라우드에 영영 안 올라가고, 다음 접속 때 예전 클라우드 세이브로 되돌아감.
- **수정**:
  - `loadState()`는 클라우드와 로컬을 **둘 다 읽어서 더 많이 진행된 쪽**을 고른다(기준: `lifetimeEarned` → 같으면 `savedAt`). 클라우드가 비어 보여도 로컬 진행을 잃지 않고, 다른 기기가 더 진행했으면 그쪽이 선택된다.
  - `saveState(state, {allowReset})`에 **빈 상태 덮어쓰기 방지** 안전장치: 저장하려는 상태가 새 게임 수준(`tables<=1 && lifetimeEarned<=0 && 운영진 0 && 프레스티지 0`)인데 이 세션이 진행된 세이브를 본 적 있으면 **로컬·클라우드 모두 쓰지 않고 거부**하고, game.js가 "새로고침하면 돌아옵니다" 토스트를 띄운다. 초기화·가져오기·패치 초기화는 `allowReset: true`로 통과.
  - `localSaveRaw()`가 **누적 수익이 줄어드는 저장**을 감지하면 직전 상태를 `...save.v1.backup`에 보관. 설정 탭에 백업이 지금보다 나을 때만 "🛟 백업 복구" 상자가 뜬다(`GameBackend.getBackup()`).
  - 모바일 사파리는 `beforeunload`가 잘 안 떠서 **`pagehide`에서도 저장**. 저장이 3회 연속 실패하면 토스트로 알림.
- **화면 확대**: iOS는 viewport의 `user-scalable=no`를 무시한다 → 버튼·카드류에 **`touch-action: manipulation`**(탭·스크롤은 그대로, 더블탭 줌만 차단 — 연타가 씹히지 않는다), `gesturestart/change/end` preventDefault(핀치 줌), 그리고 **버튼 바깥에서만** 더블탭 preventDefault(옛 iOS 대비).
- **검증**: 로그인 상태 + 클라우드 빈 문서 + 로컬에 진행 있음(예전이라면 전부 날아가던 조합)으로 띄워서 누적 1,024만·반복 #31이 그대로 살아있는 것 확인. 테스트 스텁의 `auth.currentUser`를 실제처럼 uid 있는 상태로 바꿔서 앞으로도 이 경로가 회귀 테스트된다.

### 2026-09-18 (9차) — 특성 시너지(딜링·처세·접객·승부사·흥·두뇌)
- 사용자 요청("시너지가 있으면 더 재밌을 것 같은데 뭐 딜링 처세 이런 거") → 역할 종류 수로 붙던 밋밋한 시너지를 **특성 세트 보너스**로 교체.
- `D.traits`: 6종(딜링=전체 수익 / 처세=방문객 / 접객=바·시설 / 승부사=대회 전투력 / 흥=💎말풍선 빈도 / 두뇌=오프라인 효율), 단계는 **3/5/7명**. 10명×2특성=20칸이라 2/4/6으로는 거의 다 켜져서(실측 전 특성 1단계) 3/5/7로 올림.
- 특성 배정: 역할에서 1개(`roleTrait`) + id 해시로 고정 1개. 손으로 정하고 싶으면 `D.traits.overrides`에 id별로 적으면 됨(실명 SSR 6명은 이미 지정).
- 적용 지점: `incomeMultiplier`(income) / `visitorsPerHour`(visitor) / `fixtureIncome`(fixture) / `tournamentPowerParts`(power) / `scheduleNextBubble`(bubble) / `offlineEfficiency`(offline). `deploymentSynergyMultiplier()`는 **활성 단계 합 × 5% + 풀스쿼드 20%**로 재정의(장착효과·대회 전투력에 곱해짐).
- UI: 편성 스테이지에 시너지 칩(인원/단계/보너스, 미달이면 "n/3"), 카드 왼쪽 위에 특성 이모지 2개, 상세 모달에 특성 칩(다음 단계까지 몇 명 남았는지). `⚡ 자동 편성`은 전투력 상위 조합 + 특성 1종/2종 몰아주기 조합을 만들어 점수(장착효과 합 × 시너지 × 수익계 특성 가중)가 가장 높은 걸 고름.

### 2026-09-18 (8차) — 배포 캐시 섞임 사고, 도감을 쿠키런식 편성 화면으로
- **배포 직후 매장이 안 그려지던 사고**: 사용자 화면에 빈 매장 + 밋밋한 HUD + "성장 미션 1/20"(새 데이터는 21단계). 원인은 **index.html만 새로 받고 js/css는 브라우저 캐시의 옛 파일**이 쓰인 것 — 옛 game.js의 `renderHUD()`가 지금은 없는 `#diamond-rate`를 만지다 매 틱 예외 → `refresh()`가 중단돼 `renderScene()`이 한 번도 안 불림. **해결: 모든 로컬 css/js 주소에 `?v=7`**(index.html + 모듈이 공유하는 `./firebase-init.js?v=7` 임포트까지 같은 번호로). **배포할 때마다 이 숫자를 올릴 것.** 덤으로 `setText`/`setHtml`이 요소가 없으면 조용히 넘어가게 해서 같은 상황에서도 게임 전체가 멈추지는 않게 했다.
- **도감 = 편성 화면(사용자가 쿠키런 킹덤 스크린샷을 주며 "내가 원하는 형태야")**:
  - 맨 위 **편성 스테이지** — ⚔️ 대회 전투력 + 편성 10칸(초상화·★·이름, 빈칸은 점선) + 편성/시너지 요약. 칸을 누르면 상세 모달.
  - **편성 프리셋 5개**(`state.squads` 5개 배열 + `activeSquad`, `D.deployment.presets`). 번호를 누르면 팀 교체(각 버튼에 인원 수). 예전 세이브의 `deployedIds`는 1번 팀으로 이관되고 `deployedIds` 필드는 삭제. `squadList()`가 지금 팀을 돌려주고 `deployedIds()`/`toggleDeploy()`/`autoDeployIfRoom()`/`quickDeploy()`가 전부 이걸 쓴다. 프레스티지 keep 목록도 squads/activeSquad로 교체.
  - **카드 목록**: 한 줄 4~5장(minmax 76px), 등급색 그라데이션 카드 + ★칩 + 편성중이면 금테, 카드 전체 탭 = 상세, 아래 띠 = 편성/해제. 정렬은 편성중 → 보유 → 등급 → 별.
  - **하단 고정 바**(sticky)에 ⭐ 일괄 승급, 자동 편성은 스테이지 오른쪽으로. 예전 `codex-actions`/`codex-deploy-summary`는 제거.
- **검증**: Playwright로 편성 10/10·시너지 +38% 표시, 프리셋 전환(1번 10명 ↔ 2번 0명 → 자동 편성 후 10명), 카드 `deployed` 수와 편성 인원이 항상 일치(5→5, 7→7, 정원 초과 안 됨), 하단 바 아래로 카드가 비치지 않음, 페이지 에러 0건. **주의**: 테스트 스크립트의 `page.route` 글롭은 `?v=` 때문에 `**/js/account.js*`처럼 `*`를 붙여야 가로채진다.

### 2026-09-18 (7차) — 네비 4묶음 정리, 🏆 홀덤 대회+트로피, 💎 말풍선(금고 삭제), 오프라인 2시간, 연속 뽑기, 테이블 착석, UI 흔들림
- **네비 재구성(사용자: "업그레이드가 너무 무질서하게 나뉘어 있다", "리뉴얼은 네비에서 빼자")**: 7칸 → 4묶음 `NAV_GROUPS`(game.js). 🏗 업그레이드(테이블·시설·직원·인테리어·💎영구) / 👥 운영진(스카우트·도감) / 🏆 대회(대회·트로피 상점) / 💎 상점. 세부 탭은 시트 머리줄의 칩(`#sheet-subtabs`, 여러 개일 때 제목 대신 표시)으로 고르고, 묶음별 마지막 세부 탭을 `state.ui.lastSub`에 기억. 네비 버튼은 `data-group`, HUD 버튼은 `data-tab`. 탭 id 변경: `store`→`tables`(확장·방문객 흡수)+`fixtures`, 옛 `staff`(가챠+고용)→`scout`(가챠)+`staff`(고용), 상점의 영구 업그레이드 → `permanent`. **리뉴얼은 좌상단 ✨ 배지로만 연다**(`STANDALONE_TITLES`).
- **다이아 금고 삭제 + 패시브 다이아 적립 삭제(사용자: "금고 때문에 다이아 수급이 너무 쉽다")**: `D.fixtures`에서 vault 제거, `diamondPerSecond()`·오프라인 다이아·HUD "/분" 제거, 세이브의 `fixtures.vault`는 로드 때 삭제. 3D의 금고 자리는 **🏆 대회 접수대**(`type: "tournamentDesk"`, 탭하면 대회 탭, 우승 수만큼 트로피 컵 최대 5개).
- **💎 다이아 말풍선(사용자 요청)**: 테이블에 앉은 손님 머리 위에 말풍선 → 터치하면 💎1~2(`D.diamondBubble`). game.js `tickDiamondBubble()`이 25~55초(운영진 "diamond" 장착효과만큼 단축, 효과 라벨도 "다이아 말풍선"으로 변경)마다 `PubScene3D.spawnDiamondBubble()` 호출, 씬은 터치를 **화면 거리로 판정**(`pickBubble`, 최소 34px)해 `PubScene3D.onDiamondBubble()`로 알림. 말풍선 뜬 손님은 사라질 때까지 안 일어남, 최대 3개, 12초. 성장 미션 t10 "다이아 금고" → "다이아 말풍선 터치"(이 미션일 땐 6초마다 뜸). 일일/반복 퀘스트에 `diamondBubble` 추가.
- **🏆 홀덤 대회 + 새 재화 트로피(사용자: "홀덤펍 대회 같은 걸 넣어서 다른 재화를 만들고 쓸 곳을 고민")**: `D.tournament` 5단계(동네 30명·2분 → 월드 1000명·40분). 한 번에 1개, BB 참가비(부스트 뺀 수익 × 대회 분 × 30초). **순위는 참가 순간 결정**: `topFrac = random^(0.5 + 1.5 × 전투력/추천)`, 탈락 시점 = 남은 인원 곡선(`fieldCurve`)이 내 순위 아래로 내려가는 때 → 진행 중엔 남은 인원만 보여주고 끝나면 결과 카드에서 보상 받기. 전투력 = 배치 운영진(등급 파워 × 별 × 시너지) + 테이블×2 + 리모델링×1, × 대회 훈련. 이전 단계 상금권(상위 15%, 최소 9위) 입상 시 다음 단계 해금. **추천의 3배 초과면 보상 감소·다이아 없음**(쉬운 대회 파밍 방지). HUD 부스트 줄에 🏆 남은 시간/결과! 알약(탭하면 대회 탭), 네비·세부 탭 알림 점. 대회 상태·트로피는 리뉴얼해도 유지(`doPrestige` keep 목록에 추가).
- **트로피 사용처** `D.trophyShop`: 🌙 영업시간 연장(오프라인 한도 +30분, 20레벨=12시간) / 🎯 대회 훈련(전투력 +8%) / 🏛️ 명예의 전당(전체 수익 +4%, `incomeMultiplier`에 가산) / 🎫 뽑기권 교환(🏆25, 하루 5장, `state.trophyDaily`) / 🧩 운영진 조각(운영진 상세 모달에서 등급별 🏆3~35).
- **오프라인 수익 처음엔 2시간(사용자 요청)**: `D.offline.baseMaxSeconds` 2h + 영업시간 연장. 한도를 넘겨 자리 비웠으면 오프라인 팝업에 안내.
- **반복퀘스트 난이도 하향(사용자 요청)**: 시작값·증가율·상한 전부 낮춤(리모델링 25→10, 시설 20→8, 직원·장식 15→6, 가챠 5→3, 부스트 2→1, 테이블 4→2, 매출 150초/최대 600초 → 60초/최대 240초). **이미 받아둔 퀘스트도 `repeatState()`에서 새 상한으로 줄임.** 대회 출전·말풍선 퀘스트 추가.
- **성장 미션 21단계**: t14 뒤에 `t14b` "첫 대회 출전" 추가. `D.tutorialVersion` 2 + `state.tutorial.v` — v1 세이브가 step ≥ 14면 +1 해서 같은 보상을 두 번 받지 않게(20단계 다 끝낸 세이브는 21 = 완료). **앞으로 튜토리얼 중간에 단계를 끼우면 버전 올리고 이 이관 로직을 추가할 것.**
- **🔁 연속 뽑기(사용자 요청)**: 스카우트 탭에 묶음(1/10/30회씩)·멈춤 조건(SSR / SR 이상 / 새 운영진 / 재화 다 쓸 때까지)·🎫뽑기권만 쓰기 설정(`state.ui.autoPull`) + 시작 버튼. 결과창에 누적 요약(회차·총 횟수·등급별·NEW·쓴 💎/🎫), "⏹ 멈추기"/"다시 연속 뽑기", 결과창 닫기·배경 탭 시 즉시 정지. 단발 뽑기 결과창엔 "n회 더". 뽑기 본체는 `executePull()`로 분리(재화 부족이면 아무것도 안 씀), 새 운영진이 나왔을 때만 `sceneDirty`(연속 뽑기 중 매장 전체 재렌더 방지), 연속 뽑기 중엔 카드 등장 애니메이션을 짧게.
- **방문객만큼 테이블 착석(사용자: "방문자는 많은데 테이블에 앉는 손님이 너무 없다")**: 원인은 손님 상한 16명 + 1~2.8초마다 1명 + 포커 10~22초라 실제론 8명 남짓만 있었음. → `tableOccupancy()` = 테이블당 방문객/16을 0.35~0.95로 제한해 스냅샷으로 넘기고, 씬이 `customerTargets()`(포커 좌석 × 비율, 기타 기물 7명)만큼 모자란 쪽으로 빠르게 들여보냄(`updateSpawning`). 포커 착석 35~80초, 첫 렌더 때 목표의 85%를 바로 앉혀둠(`prefilled`), 손님은 그림자 끔, 손님 상한 72명(코어 ≤4 / 메모리 ≤3GB) / 128명. 테이블 탭에 "좌석 약 n% 채움" 표시. 실측: 테이블 12개(82석)·마케터 6 → 목표 78, 앉은 손님 62.
- **숫자 바뀔 때 UI 흔들림(사용자 요청)**: HUD 재화를 한 장짜리 **지갑**(`.wallet` 184px 고정, BB+초당 / 💎＋ / 🏆)으로 묶고 숫자는 말줄임, 카드 버튼 폭 102px 고정, 퀘스트 배지 폭 고정·진행 버튼 76px, 부스트 알약 최소 폭 + `m:ss` 표기(`formatClock`), HUD 텍스트는 바뀔 때만 씀(`setText`/`setHtml`).
- **검증**: Playwright(`chromium-1234` 실행 파일 직접 지정, account.js/firebase-init.js를 가짜 모듈로 가로채 실서버 안 씀) + 로컬 정적 서버. 새 게임 / 옛 형식 세이브(금고 Lv15, 튜토리얼 v 없음, 리모델링 목표 25, 5시간 전 저장) / 360px 폭 시나리오: 오프라인 "2시간까지만" 안내, 퀘스트 4/10으로 줄어듦, 튜토리얼 완료 유지, 4묶음·세부 탭 전환, 버튼 폭 102px 일정, 대회 참가→fastForward→결과→트로피 지급, 연속 뽑기(뽑기권 10장 먼저 → 다이아) 멈추기, 말풍선 화면 좌표 클릭 → 💎 증가, 360px에서 지갑·프로필 안 겹침·세부 탭 5개 한 줄. 페이지 에러 0건. `window.__pubDebug()`에 착석 수·목표·말풍선 화면 좌표 추가.
- **다음 할 일**: (1) 실기기에서 손님 128명일 때 프레임 확인 — 느리면 `MAX_CUSTOMERS`/`LOW_END` 기준 조정, (2) 대회·트로피 수치(보상/비용/추천 전투력) 실플레이 피드백 받아 조정, (3) 말풍선 다이아 수급량이 적당한지 확인(현재 켜두고 다 누르면 시간당 약 100개), (4) 이전 세션의 결제 테스트 모드·서버 검증·Firestore 규칙 문제는 그대로.

### 2026-09-17 (6차) — 반복퀘스트 전수 점검("매출 올리기"가 안 오른다는 제보)
- **리뉴얼하면 "매출 올리기" 진행도가 0에 멈추던 버그**: 진행도를 "퀘스트 받을 때의 이번 회차 누적 수익(`totalEarned`)"과의 차이로 셌는데, 리뉴얼하면 `totalEarned`가 0이 되고 반복퀘스트는 유지돼서 예전 누적을 다시 넘을 때까지(사실상 영원히) 0으로 표시됨. 반복퀘스트가 리뉴얼로 다시 뽑히지도 않아 빠져나갈 방법도 없었음 → 줄지 않는 `lifetimeEarned` 기준(`startLifetime`)으로 변경. 예전 형식(`startEarned`) 퀘스트는 그동안 번 만큼 살려서 자동 변환(`earnProgress`).
- **매출 목표가 부풀던 문제**: 목표 = 현재 초당 수익 × 초 인데, 부스트(최대 x2·x3·x2·x2 중첩)가 켜진 순간에 뽑히면 목표가 몇 배가 돼 부스트가 끝난 뒤엔 한참 걸림 → 부스트를 뺀 기본 수익으로 목표 계산.
- **광고 부스트가 "부스트 사용" 퀘스트/일일미션에 안 세지던 문제** → `trackMission("boost")` 추가. 설명 문구 "칩 {n} 벌기" → "{n} BB 벌기".
- **매장 시설·직원·인테리어 버튼 탭이 가끔 씹히던 문제**: 시트가 열려 있으면 0.5초마다 목록 카드를 `innerHTML`로 통째로 새로 만들어서, 누르는 도중에 버튼이 교체되면 클릭이 안 됨(구매도 퀘스트 진행도 안 됨) → `keyedCards`로 카드는 구성이 바뀔 때만 만들고 평소엔 내용만 갱신, 버튼 글자도 바뀔 때만 다시 씀(`setHtml`), `.btn > *`는 `pointer-events:none`으로 탭 대상이 항상 버튼 자체가 되게. (테이블 탭·뽑기 버튼은 원래 고정 버튼이라 해당 없음)

### 2026-09-17 (5차) — 뽑기 1/10/30회+뽑기권 자동 무료, 10성·운영진 효과, 일괄승급/빠른배치, 자동 업그레이드 제거
- **x1/x10/x100/MAX 배수 버튼을 메인 화면 → 시트 안으로 이동**(시트 제목 아래 줄). 칩으로 사고파는 탭(매장·테이블·운영진·인테리어)에서만 보이고 도감·리뉴얼·상점·설정에선 숨김(`QTY_TABS`).
- **자동 업그레이드 기능 전면 삭제**(버튼, `autoUpgradeTick`, `D.autoUpgrade`, 하단 컨트롤 바 자체). 이에 따라:
  - 성장미션 14단계 "자동 업그레이드 켜기" → **"운영진 1명 이상 배치"**(`deployedCount` 스탯 추가)로 교체.
  - 광고 보상 "업그레이드 가속" → **"수익 2배 5분"**(`D.boosts.adBoost`, 쿨타임 20분)으로 교체.
- **사장님 선물 받기 미션 삭제**: 일일미션·반복퀘스트 풀에서 제거, 성장미션 5단계는 번호가 밀리지 않게 "서빙 직원 1명 고용"으로 교체. 예전 세이브의 오늘 일일미션에 없어진 미션이 있으면 다시 뽑음.
- **반복미션에서 매장 확장 삭제**(매장 최대 크기가 있어서). 같은 이유로 테이블 구매 미션은 이미 24개면 안 나오고, 목표 개수도 남은 자리만큼으로 제한. 진행 불가 상태가 된 반복퀘스트는 자동으로 다시 뽑음. 일일미션도 동일.
- **뽑기 버튼 1회/10회/30회**(`D.gacha.pullOptions`, 30회는 💎2040 약 15% 할인). 10장마다 R 이상 1장 확정(30회=3장). **무료 뽑기권이 1/10/30장 이상이면 해당 버튼이 초록색 "무료 n회"로 바뀌어 뽑기권으로 뽑음.** 별도 뽑기권 버튼은 없애고 가챠 박스 오른쪽 위에 🎫 개수만 표시. 뽑기 버튼은 탭이 0.5초마다 다시 그려져도 탭이 씹히지 않게 한 번만 만들고 내용만 갱신.
- **운영진 최대 10성**(`maxStar: 10`, 등급별 승급 조각 곡선을 9단계로 연장). **별 표시는 5칸: 빈 별 → 은별(★1~5) → 금별(★6~10, 왼쪽부터 금색)** (`starsHtml`).
- **운영진마다 보유효과 + 장착효과**(`D.dealerEffects`): 장착효과(배치 중일 때)는 역할별 — 영업=전체 수익, 서비스=바·시설 수익, 이벤트=다이아 적립, 인맥=방문객. 보유효과(보유만 해도)는 id로 고정된 6종 중 하나(전체 수익/바·시설/다이아/방문객/오프라인 효율/선물 보상), 크기는 장착효과의 15%. 수치 = 등급 기본값 × 별 배율 × 종류별 배율(수익 환산 가치를 비슷하게 맞춤). 배치 시너지는 장착효과에만 곱해짐. 기존 "배치된 인원 보너스를 전부 수익에 더하던" 방식은 이 구조로 대체됨. 도감 상세 팝업에 두 효과 표시(미보유도 ★1 기준 미리보기).
- **도감 일괄 승급**(조각 충분한 사람 전원을 끝까지, 결과는 알림 1번) + **빠른 배치**(역할별 최강 1명씩 넣은 조합 vs 전투력 상위 10명 조합 중 장착효과×시너지 점수가 높은 쪽).
- **30회 뽑기 결과창 "확인" 버튼이 안 보이던 문제**: 카드 30장이 한 줄 3장씩 세로로 길게 늘어져 확인 버튼이 결과창 맨 아래로 밀려났음 → 카드 목록만 결과창 안에서 스크롤, 제목·확인 버튼은 항상 보이게. 10장 이상이면 한 줄 5장으로 작게(`.gacha-results.many`).
- **바닥 안내 문구 겹침**: 3D 씬 위의 "매장이 가득 찼어요" 문구(`#floor-note`)가 시트를 열어도 남아서 시트 내용을 가렸음 → 시트가 열리면 숨김. 매장이 최대 크기(테이블 24개)면 "확장해보세요" 대신 "테이블을 최대로 채웠어요!".
- **별 구분**: 빈 별은 테두리만 있는 ☆, 은별은 진한 테두리를 넣어 빈 별과 헷갈리지 않게.
- **업그레이드할 때마다 뜨던 알림 삭제**(테이블 구매·리모델링·매장 확장·시설·직원·장식품·인테리어 등급·영구 업그레이드·개별 승급). "다이아 부족", "최대 확장" 같은 안내는 유지.
- **전 계정 게임 데이터 초기화(사용자 요청)**: 서버에서 직접 지울 권한이 없어서 `D.dataResetVersion`(현재 1) 방식 — 세이브의 `dataResetVersion`이 이 값보다 낮으면(=이번 패치 이전 세이브 전부) 접속 시 버리고 새로 시작해 곧바로 클라우드에 덮어씀. 로그인 계정 자체는 유지. **앞으로 또 전체 초기화가 필요하면 이 숫자만 올리면 됨.**

### 2026-09-17 (4차) — 손님 사라짐 버그, 휴대폰 매장 안 보임, 용량 절반, 뽑기 레벨 바
- **업그레이드하면 손님이 전부 사라지던 버그**: `scene3d.js`의 `update()`가 매장을 다시 그릴 때마다(업그레이드/구매/배치 등 거의 모든 상태 변화) `customers = []`로 손님을 통째로 지우고 있었음 → 같은 자리 id의 새 자리로 손님을 옮겨 붙이고, 자리가 없어졌으면 걸어 나가게 변경. 이를 위해 바 스툴 5개가 전부 같은 id(`bar`)였던 것도 `bar0`~`bar4`로 분리.
- **휴대폰에서 매장이 전혀 안 보이던 문제**: `init()`이 클라우드 세이브 불러오기를 끝까지 기다린 뒤에야 매장을 그리는데, 정식 Firestore SDK가 휴대폰 네트워크에서 연결을 오래 기다리며 멈춤(배포 사이트를 휴대폰 에뮬레이션으로 열어보니 화면이 뜨기까지 약 35초). → (1) Firestore를 **Lite판**으로 교체(REST 방식이라 실패해도 빨리 끝남, 우리는 문서 읽기/쓰기만 써서 기능 차이 없음), (2) `backend.js`의 클라우드 읽기/쓰기에 **최대 6초 제한**, 넘으면 이 기기 로컬 캐시로 시작. 이때 다른 기기의 더 최신 진행을 덮어쓰지 않도록, 클라우드를 못 읽고 시작한 세션은 첫 저장 전에 클라우드 `savedAt`을 확인해서 더 최신이면 덮어쓰지 않음. 로컬 캐시에 소유 계정(uid)을 기록해 다른 계정의 로컬 데이터를 물려받지 않게 함(기록 없는 예전 캐시는 본인 것으로 간주). 같은 로컬 환경 기준 로딩 35초 → 17초.
- **용량 절반**(기능 변화 없음): Three.js를 같은 버전의 min판으로(1.27MB→670KB), Firestore 정식판→Lite판(440KB→118KB), PortOne 결제 SDK(240KB)는 페이지 로드 때 안 받고 실제 결제를 처음 시도할 때만 동적으로 불러옴. 첫 로딩 JS 다운로드 약 2.5MB → 1.3MB. 한글 웹폰트(Jua/Gowun Dodum)는 화면에 나온 글자 조각만 받는 방식이라 그대로 둠(바꾸면 게임 느낌이 달라짐).
- **뽑기 레벨 경험치 바**: 운영진 탭 가챠 박스에 "뽑기 레벨 [바] n/20회"(최대 레벨이면 MAX)와 현재 레벨의 SSR/SR/R/U 확률 표시.
- **반복퀘스트가 잘려서 안 보이던 문제**: 배지 글자를 조금 줄이고(제목 11px/설명 9.5px) 말줄임(…) 대신 줄바꿈으로 전체 문장이 보이게 함, 배지 폭 최대 300px.
- **시트 X 버튼이 안 눌리던 문제(도감 등)**: 3차에서 사이드 레일을 재화 열(`.hud-right`) 안으로 옮기면서, 시트가 열려 레일이 숨겨져도 그 긴 열의 빈 영역이 시트 오른쪽 위 X 버튼을 덮어 터치를 가로챘음(부스트가 켜지면 완전히 가려짐) → `.hud-right`는 `pointer-events:none`, 자식만 `auto`.
- 운영진 탭의 배치 요약 글자가 무료 뽑기권 버튼에 가려지고 보라 배경에서 안 보이던 것 수정.
- 저장 데이터에 로스터에 없는 운영진 id가 배치돼 있어도 매장 렌더가 터지지 않게 `deployedIds()`에서 걸러냄.

### 2026-09-17 (3차) — 로스터 50명 확장, 테스트 모드 전면 개방, 프로필/닉네임, BB 화폐, UI 정리
- **로스터 50명(+신화 1명)으로 확장**: 사용자가 준 실명 10명은 **전부 SR 이상**으로 올림(SSR: 강민혁·정태규·김혜서·손성민·윤혜연 / SR: 최유진·김태웅·조연주·강지석·이지형). 랜덤 이름 40명(`ex01`~`ex40`)을 N 14 / U 12 / R 8 / SR 4 / SSR 2로 채움. 최종 분포 N14 / U12 / R8 / SR9 / SSR7. 기존 실명 10명의 id는 그대로 유지해서 이미 뽑은 사람은 등급만 올라가고 보유 상태는 유지됨.
- **신화 등급 이름 "왕회장" → "구현모"**(id도 `chairman` → `hyunmo`, 상점 항목까지 같이 변경). 이미 왕회장을 산 계정이 있었다면 그 보유 기록은 id가 바뀌어 사라짐(테스트 단계라 무시).
- **🧪 테스트 모드 전면 개방(사용자 요청: "결제 연결 안 돼 있어도 일단 지급, 나중에 결제 설정하고 막자")**: `js/shop.js`의 `checkout()`은 `payment.storeId/channelKey`가 비어 있으면 결제창 없이 즉시 `{ok:true, test:true}`를 돌려줌 → 다이아 패키지·스타터팩·월정기권·신화 운영진·광고제거 전부 무료로 지급됨. `js/ads.js`도 `ads.network`가 비어 있으면 광고 없이 즉시 보상. 테스트 지급일 때는 토스트 앞에 "🧪(테스트 지급)"이 붙음. **실서비스 전 반드시 이 두 분기를 지우거나 막아야 함** — storeId/channelKey를 채우면 결제 쪽은 자동으로 실결제로 전환되지만, 서버 검증 없는 문제는 여전함.
- **부스트 중복 구매 차단**: 풀하우스 러시(💎30, 5분)가 적용 중일 때 버튼이 "적용중"으로 바뀌고 비활성화, `activateBoost()`에서도 이미 활성인 부스트는 재발동을 막음(다이아 낭비 방지).
- **화폐 단위 BB**: `formatChips()`가 " BB"를 붙이고, 상단 잔액/초당 수익/테이블 수익 표시도 전부 BB로 통일. 아이콘 그래픽은 그대로 둠.
- **상점 탭을 하단 네비게이션에 추가**(💎 상점, 7번째). 다이아 옆 ＋ 버튼도 그대로 유지.
- **성장/반복 미션 배지**: 2차에서 너무 작게 줄여서 제목·설명이 잘려 안 보이던 문제 → 폭 250px, 글자 12px/10px로 키우고 설명도 한 줄로 다시 표시. **배지를 탭하면 그 미션을 수행하는 탭으로 바로 이동**(`goToQuest()`, 퀘스트의 stat/action → 탭 매핑 `QUEST_TAB_MAP`, 선물/부스트/출석은 해당 모달을 엶). 받기 버튼 클릭은 `stopPropagation`으로 이동과 분리.
- **프로필 모달 신설**(좌상단 프로필 탭): 닉네임(기본값=로그인 아이디, 2~12자로 변경 가능, `state.nickname`), 레벨/칭호/XP, 매장 현황 요약 10종(초당 수익, 보유 BB, 다이아, 누적 수익, 테이블, 방문객, 보유 운영진, 배치, 가챠 레벨, 프레스티지 배율).
- **프로필 레벨업 다이아 보상**: 레벨이 오를 때마다 `5 + 레벨×2` 다이아 지급(`D.level.diamondRewardBase/PerLevel`), `state.profileRewardedLevel`로 중복 지급 방지, 매 틱 `checkProfileLevelReward()`에서 확인.
- **손님 착석**: 테이블당 최대 좌석 8 → **9**(`D.table.seatsMax`). 손님이 홀덤 테이블로 가게 되면 **안쪽 테이블(인덱스가 작은 = 입구에서 먼 뒷줄)부터** 채움. 단, "테이블로 갈지 바/소파/다트/주크박스로 갈지"는 기존처럼 전체 빈자리 중 무작위로 먼저 정함 — 무조건 테이블 우선으로 하면 테이블이 24개×9석이 다 찰 때까지 바/소파가 영영 비어버리기 때문.
- 남아 있던 "딜러" 문구(일일 미션·성장 미션·반복 퀘스트 텍스트)도 "운영진"으로 교체.
- **UI 겹침 정리**(Playwright 스크린샷으로 찾은 것들):
  - 사이드 레일(선물/출석/미션/부스트)이 `top:104px` 고정이라 다이아 표시 아래 부스트 알약과 겹쳤음 → 레일을 `.hud-right` 열 안으로 옮겨 재화·부스트 표시 아래로 자연스럽게 흘러가게 함(부스트 2개 켠 상태에서 알약 아래끝 168px < 레일 위끝 177px 확인).
  - 토스트가 항상 화면 하단 고정이라 시트/모달의 버튼을 가렸음 → 시트가 열리면 시트 위로, 모달이 열리면 화면 맨 위로 이동(`body:has(.modal:not(.hidden))`). 긴 문구는 잘리지 않고 줄바꿈.
  - 부스트 모달 "적용중" 버튼이 두 줄로 찢어지던 것 → `nowrap` + 최소 폭.
  - 프로필 닉네임이 길면 넘치던 것 → 말줄임.
- **운영진 초상화가 전부 똑같이 나오던 버그**: `js/portraits.js`의 외형 표가 옛 로스터 id(minsu, jieun…)로만 되어 있어서 새 51명 전원이 기본 얼굴로 나왔음 → id를 해시해 피부/머리색/머리모양/옷/액세서리를 결정적으로 고르는 방식으로 교체(같은 사람은 항상 같은 얼굴, 등급이 높을수록 화려한 액세서리). U/신화 등급 테두리 색도 추가.
- **다음 할 일**: (1) 사용자가 실제 기기에서 또 겹치는 곳을 보면 캡처로 받아 추가 정리, (2) 테스트 모드 분기 실서비스 전 제거, (3) 운영진·매장 등 0.5초마다 다시 그려지는 탭은 버튼이 계속 새로 만들어져 아주 드물게 탭이 씹힐 수 있음(기존부터 있던 구조) — 문제 되면 부분 갱신 방식으로 바꿀 것.

### 2026-09-17 (2차) — 딜러→운영진 전면 개편: 배치/시너지, 가챠 레벨, 마케터 재설계, 영구업그레이드, 광고, 유명인 이벤트
큰 요청 하나를 여러 차례에 걸쳐 받아서 한 세션에 전부 반영함. 세부 내용이 많으니 다음에 이어갈 때 이 항목 전체를 읽을 것.

- **딜러 → 운영진 명칭/개념 전면 교체**. 핵심 인사이트: 이 게임은 원래도 "딜러 배정"이 게임플레이에 영향 없는 순수 시각 요소였음(보너스는 `dealerBonusSum()`으로 보유량 전체 합산, 테이블 배정은 3D 씬 표시용일 뿐). 그래서 "테이블에 딜러가 없으면 안 된다" 요청은 **테이블에 항상 누군가 앉아있게 시각적으로 채우는 것**으로 해결: `assignedDealers()`가 배치된/보유한 운영진으로 먼저 채우고, 그래도 모자라면 `{id:null, rarity:"staff"}` 필러를 채워 넣음 → `scene3d.js`의 `DEALER_RARITY_LOOK.staff`(무채색 유니폼)로 렌더링. **빈 의자가 절대 없음**을 Playwright로 확인.
- **배치(운영) 시스템 신설**: 사용자가 명시적으로 요청한 방식 — "테이블에 배정" 개념을 없애고, 보유 운영진 중 **최대 10명("배치")**만 실제 수익에 기여하게 변경(`state.deployedIds`). 도감 카드/운영진 상세 모달에서 바로 배치 토글 가능. 서로 다른 role(영업/서비스/이벤트/인맥)을 배치 중인 인원끼리 고루 갖추면 role 하나당 +5%(`D.deployment.synergyPerRole`), 정확히 10명 꽉 채우면 추가 +20%(`fullSquadBonus`). 새로 뽑은 운영진은 "자동배치" 설정이 켜져 있으면 자리가 남는 한 자동으로 배치됨(`autoDeployIfRoom`).
- **로스터를 사용자가 준 실명 10명으로 전면 교체**: 강민혁·정태규(SSR) / 김혜서·손성민(SR) / 윤혜연·최유진(R) / 김태웅·조연주(U) / 강지석·이지형(N). **등급/역할 배정은 내가 임의로 정한 것**이니 마음에 안 들면 바꿔달라고 요청할 것(`js/data.js`의 `dealerRoster`). 로스터 id가 전부 바뀌어서 **기존 플레이어의 운영진 컬렉션은 이번 업데이트로 리셋됨**(다른 진행상황은 유지) — casino-tycoon 자매 프로젝트에서 로스터 id를 바꿀 때마다 매번 같은 트레이드오프를 감수했던 전례를 따름.
- **가챠에 "가챠 레벨"(1~10) 신설**: 사용자가 준 확률표(SSR/SR/R은 레벨별로 다르고 U는 20% 고정, N은 나머지)를 `D.gacha.levelTable`로 그대로 반영. 누적 20회 뽑을 때마다 레벨업(`D.gacha.pullsPerLevel`). 등급도 기존 N/R/SR/SSR 4종에서 **N/U/R/SR/SSR 5종**으로 확장(U="고급", 기존 로스터를 5개 구간으로 재분배). 10연차 "희귀 이상 확정" 로직이 등급 index 비교 방식이 아니라 "common이 아니면 확정 충족"으로 되어 있던 **잠재 버그**를 이번에 같이 고침(U가 생기면서 그 버그가 실제로 터질 조건이 됐음 — `rarityRank()` 기반으로 재작성).
- **중복 조각 필요량을 등급별로 재조정**: 기존엔 전 등급 공통 커브(`shardsPerStar`)였는데, 로스터가 작아지면서(등급당 2명) 낮은 등급이 더 자주 나와 쉽게 만성화되는 문제가 커짐 → 등급별 커브(`shardsPerStarByRarity`)로 분리, N/U가 SR/SSR보다 훨씬 많은 조각을 요구하도록 상향.
- **상점 전용 "신화" 등급 신설**: SSR 위 등급, 가챠 기본 확률표엔 없음. 상점에서 "왕회장"(**이것도 임시로 지은 이름** — 원하는 이름 있으면 바꿔달라고 요청) 1명을 다이아 아님 실결제(₩29,900)로 구매하면 즉시 보유 + 그 순간부터 낮은 확률로 가챠 풀에도 합류(`state.purchases.unlockedMythic`).
- **마케터 재설계**: 기존엔 그냥 "테이블 수익 +12%" 고정 배율이었는데, 이제 **"시간당 방문객 수"를 늘리는 역할**로 바뀜(`visitorsPerHour()`). 방문객 수는 매장 탭에 그대로 표시되고, 실제 매출에도 로그형 완만한 곡선으로 반영됨(`customerFlowMultiplier()`) — "손님이 많이 오면 많이 벌어야 한다"는 요청을 방문객이라는 명시적 지표로 구현.
- **영구 업그레이드 신설**(`D.permanentUpgrades`, 상점 탭): 매출/오프라인 효율/가챠 확률(rare+ 가중치) 3종, 다이아로 강화하며 **리뉴얼(프레스티지)해도 절대 초기화 안 됨** — 기존에도 이미 dealers/theme 등은 리뉴얼 시 유지되고 있었지만, "영구 vs 리셋"을 상점 화폐(다이아) 축에서 명시적으로 분리한 새 카테고리.
- **광고(리워드) + 광고 제거**: `js/ads.js` 신설, 결제(`js/shop.js`)와 똑같은 패턴으로 "실제 광고 SDK 없음 → 광고 준비중" 안내만 뜨게 안전하게 막아둠(`D.ads.network`가 비어있는 동안). 무료 1회 뽑기 / 업그레이드 5분 3배 가속 2종. "광고 제거"(₩3,900, 상점)를 사면 이후로 이 두 리워드를 광고 없이 바로 받음.
- **유명인 방문 특수 이벤트**: 강민혁·정태규가 25~55분 간격으로 랜덤 방문, 모달로 알림 + 보상(💰1시간치+💎25) 수령.
- **UI 정리**: 성장 미션 배너를 중앙 상단 와이드 배너 → **우측 하단의 작은 배지**로 축소(설명 텍스트는 숨기고 진행바+단계만). "직원" 탭/네비를 "운영진"으로, "딜러 도감" → "운영진 도감"으로 라벨 전면 교체.
- **매장 확장 상한**: `D.store.maxShownSlots`(24)가 3D 씬이 실제로 그릴 수 있는 한계인데 기존엔 그 이상도 확장 가능해서 "확장했다고 표시만 되고 실제로는 안 보이는" 문제가 있었음 → `tableCapacity()`를 24로 하드 캡, 다 차면 확장 버튼이 "최대 확장 완료"로 바뀜.
- **검증**: Playwright로 (1) 신규 가입 직후 테이블에 빈 의자 없이 기본 운영진이 앉아있는지 카메라 팬으로 직접 확인, (2) localStorage에 다이아 9999를 주입해 10연차 5회(50회) 실제로 뽑아서 가챠 레벨이 정확히 Lv.3(50÷20=2.5→+1=3)로 오르는지, 배치 요약이 "7/10명·시너지 +20%"로 정확히 계산되는지, 도감 카드의 배치 토글이 실제로 눌리는지까지 확인. 전 과정에서 페이지 에러 0건.
- `npm run build` 없음(빌드리스 프로젝트) — 대신 `node --check`로 전 파일 문법 검증 + 위 Playwright 시나리오로 런타임 검증함.
- **다음 할 일**: (1) 로스터 등급/역할 배정과 "왕회장" 이름이 마음에 드는지 확인받기, (2) 무료 뽑기권 지급이 실제 반복 퀘스트 완료 시점에 잘 들어오는지 실플레이로 확인(이번엔 코드 리뷰 수준으로만 확인, 20단계 튜토리얼을 다 깨는 실통합 테스트는 안 함), (3) 1차 세션에 이미 있던 미해결 사항들(PortOne 실연동, Cloud Functions 결제 검증, Firestore 보안 규칙)은 그대로 유효.

### 2026-09-17 (1차) — 회원가입/계정설정 + 수익모델(다이아 상점) 신설
- **계정 시스템**: Firebase Authentication(아이디/비번, 내부적으로 `아이디@holdem-pub-tycoon.local` 이메일로 변환) 도입. `js/firebase-init.js`(초기화) / `js/account.js`(가입·로그인·로그아웃·비번변경·계정삭제) / `js/auth-gate.js`(로그인 전 풀스크린 게이트, 로그인 성공 시 `window.HoldemGame.start()` 호출).
  - **Firebase 프로젝트는 casino-tycoon(다른 프로젝트)과 동일한 프로젝트(`jinojino-6aba2`)를 재사용**했다 — 새 프로젝트 생성 없이 바로 씀. 이메일 접미사(`@holdem-pub-tycoon.local`)와 Firestore 컬렉션 접두사(`holdemPub_`)로 데이터가 섞이지 않게 분리.
  - `js/backend.js`를 로컬 전용 → Firestore(`holdemPub_saves/{uid}`) 우선 + localStorage는 오프라인 캐시로 개편(계정별 클라우드 세이브, 여러 기기에서 이어할 수 있음).
  - 설정(⚙️) 탭에 "계정" 섹션 추가: 현재 아이디 표시, 비밀번호 변경, 로그아웃, 계정 삭제(클라우드 세이브까지 함께 삭제).
- **수익모델(상점)**: `js/data.js`에 `shop`/`payment` 설정 신설 — 방치형 타이쿤 장르 표준 구성 참고(신규 전환용 1회 한정 스타터팩 + 단계별 다이아 패키지(고액일수록 보너스%↑) + 월 정기권 구독). 광고는 이번엔 제외(사용자 결정).
  - 재화(다이아) 옆에 "＋" 버튼 신설 → 상점 탭으로 바로 이동(업계 표준 UX).
  - `js/shop.js`: PortOne(아임포트) V2 브라우저 SDK(`https://cdn.portone.io/v2/browser-sdk.js`, `PortOne.requestPayment`) 연동 코드 작성. **아직 PG 가맹점 미가입**이라 `D.payment.storeId`/`channelKey`가 빈 값이면 상점 버튼이 "결제 준비중" 안내만 뜨고 실제 결제는 막혀 있음 — PortOne 가입 후 그 두 값만 채우면 바로 실 결제로 전환됨.
  - 첫 결제 1회는 다이아 2배 지급(전환 유도), VIP 월 정기권은 구매 시 즉시 다이아 + 30일간 매일 접속 시 다이아 자동 지급 + 전체 수익 +10%(전부 프레스티지해도 유지되는 계정 단위 상태).
  - **⚠️ 중요 미해결 사항**: 지금 구조는 결제 성공 응답을 **클라이언트가 그대로 신뢰**해서 재화를 지급한다(서버 검증 없음 — 이 프로젝트는 GitHub Pages 정적 호스팅이라 서버가 없음). 즉 마음만 먹으면 브라우저 콘솔에서 재화를 위조할 수 있는 상태. **실제로 돈을 받기 시작하기 전에는 반드시** Firebase Cloud Functions 등으로 PortOne 결제 검증(`paymentId`로 조회해 실제로 결제됐는지 확인) 후에만 재화를 지급하도록 서버 로직을 추가해야 한다. Cloud Functions를 쓰려면 Firebase 요금제를 Blaze(종량제, 카드 등록 필요)로 올려야 함 — 사용자와 상의 필요.
  - Firestore 보안 규칙은 casino-tycoon 때와 마찬가지로 아직 "테스트 모드"(로그인 여부와 무관하게 열려있음) 그대로 둠 — 실서비스 전에 규칙을 조여야 하는 것도 미해결.
- `js/game.js`: 상점 탭 렌더러, VIP 상태에 따른 수익 배율/일일 다이아 지급, 계정 정보 렌더러 추가. 게임 시작을 `DOMContentLoaded` 자동 실행에서 `window.HoldemGame.start()`(auth-gate가 로그인 확인 후 호출)로 변경.
- 정적 사이트 실행 특성상(빌드 없음) 아직 `npx serve .`로 브라우저 실기동 검증은 못함 — 다음 세션에서 로컬 서버 띄워 로그인→가입→상점 구매(결제 준비중 안내 뜨는지)→계정 설정까지 Playwright로 스크린샷 검증 필요.
- **다음 할 일**: (1) 브라우저 실기동 검증, (2) PortOne 가맹점 가입 후 storeId/channelKey 반영, (3) Cloud Functions 결제 검증 로직 추가(Blaze 요금제 전환 필요 여부 사용자와 논의), (4) Firestore 보안 규칙 조이기, (5) 원하면 리워드 광고(무료 다이아) 나중에 추가 검토.

## 참고

- 이 프로젝트는 [casino-tycoon](../casino-tycoon)(Phaser 3 기반, 별개 게임)과 완전히 다른 프로젝트이지만 Firebase 프로젝트만 공유한다.
- 빌드 도구 없음 — `js/scene3d.js`만 ES 모듈(importmap으로 Three.js 로드), `js/account.js`/`js/backend.js`/`js/shop.js`/`js/auth-gate.js`도 이번에 ES 모듈로 추가됨(Firebase 모듈 SDK를 gstatic CDN에서 직접 import). `js/data.js`/`js/portraits.js`/`js/game.js`는 여전히 클래식 스크립트(`defer`) — 문서 순서가 곧 실행 순서이므로 `index.html`의 `<script>` 태그 순서를 바꿀 땐 주의.
- `file://`로 열면 모듈이 로드되지 않으므로 반드시 `npx serve .` 등 로컬 정적 서버로 열어야 한다(README 참고).
